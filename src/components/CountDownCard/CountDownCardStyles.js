import { StyleSheet, Dimensions } from 'react-native';

export default StyleSheet.create({
    cardContainer: {
        marginTop: 20,
        borderWidth: 1,
    },
    title: {
        fontSize: 16,
        color: "black",
        fontWeight: "bold",
        textAlign: "center",
    }
});